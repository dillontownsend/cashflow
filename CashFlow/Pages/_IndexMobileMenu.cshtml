<div class="lg:hidden" id="indexMobileMenu" role="dialog" aria-modal="true" x-data x-ref="mobileMenu">
    <div class="fixed inset-0 z-50" x-on:click="$refs.mobileMenu.remove()"></div>
    <div class="fixed inset-y-0 right-0 z-50 w-full overflow-y-auto bg-white px-6 py-6 sm:max-w-sm sm:ring-1 sm:ring-gray-900/10">
        <div class="flex items-center justify-between">
            <a asp-page="/" class="-m-1.5 p-1.5">
                <span class="text-2xl tracking-tight font-semibold">Cash<span class="text-emerald-600">Flow</span></span>
            </a>
            <button type="button" class="-m-2.5 rounded-md p-2.5 text-gray-700" x-on:click="$refs.mobileMenu.remove()">
                <span class="sr-only">Close menu</span>
                <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
                </svg>
            </button>
        </div>
        <div class="mt-6 flow-root">
            <div class="-my-6 divide-y divide-gray-500/10">
                <div class="py-6">
                    <a asp-page="/Account/Login" class="-mx-3 block rounded-lg px-3 py-2.5 text-base font-semibold leading-7 text-gray-900 hover:bg-gray-50">Log in</a>
                </div>
            </div>
        </div>
    </div>
</div>